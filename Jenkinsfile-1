pipeline {
    agent any

    parameters {
        booleanParam(defaultValue: true, description: 'run org.com tests', name: 'calculator')
    }

    stages {
        stage("org.com") {
            when {
                expression { return params.calculator }
        }
        steps {
              echo "Tests are running"
        }
       }
      }
}
